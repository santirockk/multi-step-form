export const getActivePage = state => state.page.activePage;

export const getOption = state => state.plan.option;

export const getOptions = state => state.plan.options;

export const getPlan = state => state.plan.plan;

export const getAddons = state => state.plan.addons;

export const getInfo = state => state.info.info;